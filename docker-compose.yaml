services:
  backend:
    build: .
    container_name: trading_backend
    ports: # <-- ADD THIS
      - "8000:8000" # <-- ADD THIS
    volumes:
      - ./src:/workspace/src
      - ./data:/workspace/data
      - ./app:/workspace/app
      - ./scripts:/workspace/scripts
    # <-- CHANGE THIS COMMAND TO RUN FASTAPI:
    command: uv run uvicorn src.engine.main:app --host 0.0.0.0 --port 8000 --reload
